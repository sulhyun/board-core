<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boardcore.dao.PostDAO">
	<select id="getCommunityList" resultType="Community">
		SELECT * FROM COMMUNITY ORDER BY CO_NUM
	</select>
	
	<select id="getPostList" resultType="Post">
		SELECT * 
		FROM POST 
		WHERE PO_CO_NUM = #{co_num}
		<choose>
			<when test="type == 'TITLE'">
				AND PO_TITLE LIKE CONCAT ('%', #{search}, '%')
			</when>
			<when test="type == 'ID'">
				AND PO_ME_ID LIKE CONCAT ('%', #{search}, '%')
			</when>
			<otherwise>
				AND (PO_TITLE LIKE CONCAT ('%', #{search}, '%') OR PO_ME_ID LIKE CONCAT ('%', #{search}, '%'))
			</otherwise>
		</choose>
		ORDER BY PO_NUM DESC
		LIMIT #{pageStart}, #{perPageNum}
	</select>
	
	<select id="getPostTotalCount" resultType="int">
		SELECT COUNT(*)
		FROM POST
		WHERE PO_CO_NUM = #{co_num}
		<choose>
			<when test="type == 'TITLE'">
				AND PO_TITLE LIKE CONCAT ('%', #{search}, '%')
			</when>
			<when test="type == 'ID'">
				AND PO_ME_ID LIKE CONCAT ('%', #{search}, '%')
			</when>
			<otherwise>
				AND (PO_TITLE LIKE CONCAT ('%', #{search}, '%') OR PO_ME_ID LIKE CONCAT ('%', #{search}, '%'))
			</otherwise>
		</choose>
	</select>
	
	<select id="getPost" resultType="Post">
		SELECT * FROM POST WHERE PO_NUM = #{po_num}
	</select>
</mapper>